# Test 5: Nested scopes
func main() {
    let a: int = 1;
    if true {
        let b: int = 2;
        if true {
            let c: int = 3;
            display a, "\n";
        }
    }
}
# Should WARN: unused 'b', unused 'c'
# Should NOT warn about 'a' (it's used in innermost scope)